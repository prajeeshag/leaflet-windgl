{
  "name": "leaflet-windgl",
  "module": "src/index.ts",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest",
    "@types/leaflet": "^1.9.18",
    "leaflet": "^1.9.4",
    "zarrita": "^0.5.1"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "build-demo": "bun build demo/demo.ts --outdir=demo/",
    "serve-demo": "bunx serve demo",
    "start-demo": "bun run build-demo && bun run serve-demo",
    "watch-build": "bun build demo/demo.ts --outdir=demo/ --watch",
    "watch-demo": "bun run watch-build & bun run serve-demo"
  }
}